declare module "dsp.js" {
    export default class FFT {
        constructor(bufferSize: number, sampleRate: number);
        forward(signal: Float32Array): void;
        spectrum: Float64Array;
    }
}